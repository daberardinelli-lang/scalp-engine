<%# app/views/admin/campaigns/edit.html.erb %>

<div class="mb-6">
  <div class="flex items-center gap-2 text-sm text-gray-500 mb-2">
    <%= link_to "Campagne", admin_campaigns_path, class: "hover:text-gray-700" %>
    <span>/</span>
    <%= link_to @campaign.name, admin_campaign_path(@campaign), class: "hover:text-gray-700" %>
    <span>/</span>
    <span class="text-gray-900">Modifica</span>
  </div>
  <h1 class="text-2xl font-bold text-gray-900">Modifica campagna</h1>
</div>

<%= render "form", campaign: @campaign %>
